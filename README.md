# pandas-notes
structured notes and code for pandas library---



# Git Commands 
- git config --global user.name ______ 
- git config --global user.email  ______
- git init
- git status
- git add . / git add < file name >
- git recover -staged <file>  (to unstage )
- git commit -m "______ "
- git log --oneline

**To allow git to track empty directory**  
  - add .gitkeep inside that directory

**To hide files** 
  - create a folder with name .gitignore , and add files inside that folder
    example -- .env , builts/ (a directory)
